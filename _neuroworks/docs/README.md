---
title: NeuroWorks product docs (mirror)
updated: 2026-07-01
tags: [neuroworks, docs, index]
---

# NeuroWorks product docs

Canonical product docs, mirrored from the repo so the second brain stays
current. Source of truth lives in the `clawbot` repo (`docs-vault-ready/`,
`QUICKSTART.md`, `README.md`).

- [[quickstart]] — download & run NeuroWorks on any PC (free local core)
- [[v0.3.0-release-notes]] — ADRS data pipeline, Omnisignal acquisition, external agent dispatch, installer
- [[api-reference]] — endpoints incl. `/api/datasets`, `/api/omnisignal`, `/api/v1/dispatch`
- [[architecture]] — internal architecture: Omnisignal → ADRS → agent learning, and the dispatch layer

See also `_progress/` for dated build progress and `_neuroworks/reflections/`
for the nightly system reflections.
