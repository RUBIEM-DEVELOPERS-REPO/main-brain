# _context/

Long-running domain / project context that's too big for `active-context.md` but isn't durable knowledge.

One file per project or domain. Update in place; this is mutable state, not append-only.
