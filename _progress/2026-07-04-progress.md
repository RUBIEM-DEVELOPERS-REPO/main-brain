
## 2026-07-04 — Hybrid workforce shipped + Paynow + cost tiering (Claude session)

**Hybrid human/agent workforce (all 5 phases, live-verified):**
- Work modes on hires + users: agent / hybrid / human (Personas + Users pages, PATCH endpoints).
- `human.request` primitive + continuing task loops: tasks pause in `waiting_on_human` with a structured, typed ask; Tasks page renders amber "Waiting on you" cards; POST human-input resumes as a continuation job. Verified end-to-end: investor-update task paused, took R2,450,000 / R380,000 figures, finished with them in the draft.
- Intellinexus: user-facing ADRS renamed; nightly reflections now publish per-template performance datasets through the Intellinexus pipeline (verified: reflections-2026-07-03, 3 golden records) + new /daily-reports page.
- Salary-aware cost: salary on users (R60k/mo → R346.10/h), human-work ledger (CSV/rows/connector ingest), GET /api/cost/workforce — human R3,634 beside agent $1.08 on the Cost page.
- Time-waste analytics: GET /api/tasks/time-analysis — agent runtime vs waiting-on-human vs human work, verdict + slowest types + longest waits, rendered on Cost page.
- Reframing: "The Intelligent Organization" (i18n, layout, onboarding, settings); AIIA → Aiia everywhere (158 mentions).

**Paynow (Zimbabwe) gateway:** lib/paynow.ts (SHA-512 hash auth), /api/payments/paynow/* (links, poll, hash-verified result webhook), payment.paynow_link/_poll primitives. Live-verified: payment created (ref 52379558), poll OK, hash valid. Test-mode quirk: authemail must be info@aiinstituteafrica.com.

**Cost tiering:** Anthropic provider now splits tiers — Haiku 4.5 for planning/extraction/normal synth, Fable only for complex handoffs (ANTHROPIC_SMALL_MODEL/ANTHROPIC_LARGE_MODEL overrides). Complexity fan-out hint pushes planners to parallel sub-agents on multi-part tasks.

**Orchestration test findings:** agents guessed connector paths from memory → 404s; connector.call now resolves endpoint names/path tails against the declared manifest and returns the endpoint list on 404.

All uncommitted. Rotation list grew: Paynow integration key (pasted in chat).

## Session continued — reflection punch list + access layers

**Reflection punch list (all 8):** the "80% ollama failure" was a telemetry artifact — steps skipped after a human.request pause persisted as 0ms failures; journal now drops never-started runs. Step retries 2→3 (adds 8s attempt). quality.check wall cap 300s→120s + fail-OPEN for non-research deliverables on grader stalls. reflection:daily now records real durations (was 0s). Reflection scheduler no longer re-fires on every restart (mtime guard — had produced 5 duplicates). Skill-picker apply threshold 15→20 (keyword-only matches no longer trigger playbooks); logging confirmed working. Peer delegation gets a 3s pre-flight health ping (was eating 12-min timeouts on dead workers). research.deep default depth 3→2, cap env-tunable.

**Access layers (proposal items 1,3,4,5):** superadmin/admin/staff roles with migration (Arthur→superadmin); lib/access.ts requireLayer gates — cost=money=superadmin; models/connectors/integrations/terminal/governance=secrets=superadmin; users=admin; approvals=admin. Salaries redacted below superadmin (read + write). Staff jobs feed department-filtered via workforce dept inference. Add-user modal = layer bundle picker with department auto-propose + sees/hidden access preview (served from /api/auth/layers). Guardrails: last-superadmin undemotable/undeletable, role changes audited. KEY RULE: token-less callers (agents/connectors/scripts) = machine context, pass gates. All verified live: staff 403 on money/secrets/users/approve, admin salary-blind, demote-last-superadmin 400. 55/55 tests.

## Sign-up approval + org management (Admin page)
Self-signup ("Request access" on Login) lands as status "pending" — cannot sign in until approved. Admin page gained: Organization overview (headcount, layers, work modes, departments), Pending sign-ups queue (approve = set access layer + department + work mode in one click; reject deletes), Active sessions with revoke (token prefixes only). Endpoints under admin-gated /api/users/*. Full flow live-verified including revoked-token invalidation.
