
## Summit Recon incident fixes (2026-07-07)
Incident: "summarize Summit Recon.xlsx in an email" task emailed a raw rescue
summary (internal JSON + leaked Alignment-check directive) after cloud synth
failed with "credit balance is too low". Four fixes, all verified:
1. openrouter.ts: provider credit exhaustion (Anthropic 400 / OR 402) now trips
   the circuit breaker (30min hold, CLAWBOT_OR_BILLING_HOLD_MS) and marks
   transient so synth falls back to LOCAL model instead of failing the task.
   billingExhausted surfaced on /api/status/llm. Verified with mock 400: local
   fallback answered, breaker tripped.
2. doc-extractor.ts + data-sources.ts (5 sites): XLSX.readFile throws "Cannot
   access file" in the xlsx ESM build (no fs wired) — switched to
   readFileSync + XLSX.read(buffer). Verified: Summit Recon.xlsx now extracts
   real sheet content.
3. agent.ts parseUserRequestFromTask: strips **Alignment check** and
   **Source-of-truth notice** planner directives from customer-facing task echoes.
4. agent.ts compactStepSummary: renders fs matches[] as a file list and doc
   `content` as text — no more raw JSON in rescue summaries/emails.
60/60 tests (5 new), typecheck clean, uncommitted.
NOTE: Anthropic provider account is OUT OF CREDITS — top up or tasks run local.

## Follow-up: "fetch failed" email task + credits restored (2026-07-07 pm)
- Root cause of the 14:51 "fetch failed" failure: delegateToPeer's poll loop
  had a bare fetch — ONE dropped connection to the busy worker threw out of
  the loop and failed the delegation, while the worker succeeded 17s later.
  Fixed both poll loops (peers.ts): 10s per-poll timeout, tolerate up to 20
  consecutive poll failures (~1min) before giving up.
- Anthropic credits topped up: verified with a real paid call (claude-haiku-4-5
  answered), breaker closed, billingExhausted=false.
- Re-ran the Summit Recon email task end-to-end: succeeded with a real
  financial summary (costs/payments/outstandings/speaker fees) and the email
  was delivered to arthurmagaya2@gmail.com via Mailjet at 15:18Z.
- 60/60 tests, typecheck clean, uncommitted.
