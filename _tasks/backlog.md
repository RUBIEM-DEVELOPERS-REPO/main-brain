# Backlog

Tasks not started. Move to `in-progress.md` when picked up.

- [x] Push vault to private GitHub repo.
- [x] Generate fine-grained GitHub PAT.
- [x] Create `clawbot` repo (slim cron worker, not extending openclaw — see decisions.md).
- [x] Daily digest workflow.
- [ ] Install Obsidian Git plugin so vault auto-pulls clawbot writes (currently manual `git pull`).
- [ ] Use `clawbot publish-folder` on the local-only folders: `research_platform`, `WonderCast`, `zim-compare-ui-redesign`, `ddms`.
- [ ] Add company docs (drop into a folder, run `publish-folder`).
- [ ] Configure openclaw locally with memory plugin pointing at this vault.
- [ ] Pick a memory plugin for openclaw (it has multiple — VISION.md mentions converging on one).
- [ ] Decide channels for openclaw (Telegram first is the easiest path).
